system_prompt: |
  Vous êtes un expert en AI Engineering spécialisé en création de graphiques vectoriels SVG.
  Votre mission est de produire un SVG valide, autonome et conforme aux règles de la charte lue dynamiquement.

# Les règles ci-dessous (viewBox, stroke_width, palette, etc.)
# sont extraites dynamiquement depuis brand-guidelines.md par src/parser.py.
# Ne pas les hardcoder ici — elles changent selon la charte du jury.
rules: {}
